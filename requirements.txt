# 核心依赖
# 注意: torch 在 Kaggle / Colab 等 GPU 环境通常已预装且与 GPU 兼容,
#      请不要无脑 pip install -r requirements.txt 覆盖, 会引发
#      cudaErrorNoKernelImageForDevice。本地开发再装。
torch>=2.1.0
numpy>=1.24
h5py>=3.9
pyyaml>=6.0
tqdm>=4.65
scipy>=1.11
tensorboard>=2.15

# 仅本地开发/调试用
# scipy 用于 PSD 数值对照测试
